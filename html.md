```html

<div> <!--- opening tag --->
hello <!---children--->
</div> <!--- closing tag --->

<div class = "my-div"> <!---Tags can have attributes--->

Some important props are:
 - `class` : gives a CSS class to your element
 - `onChange`: fireds an event when your element's value changes
 - `onClick`: fireds an event when your element is clicked


### elements

`<div>` : standard box with notion pre-defined
`<img>` : image.. t can "src" and" alt"
`<header>`, `<footer>`, `<main>`

`<a href = "google.com">I am a link</a>` : link ti has an hred prop

`<button>`
`<input>`